"""kilix-tui — the text-native desktop.

The desktop is an index and a power switch. Every entry composes a tool that
already exists; the moment this package contains an application it has failed
its own charter. The entry point is `kilix-tui/main.py` at the repository
root — deliberately not under `tools/`, because those are what the desktop
launches.
"""
